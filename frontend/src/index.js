import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Adicione seu CSS personalizado aqui, se necessário
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

